---
import { ViewTransitions } from "astro:transitions";
import { SITE } from "@/config";
import "@/styles/global.css";
import Analytics from "@/components/Analytics.astro";

const googleSiteVerification = import.meta.env.PUBLIC_GOOGLE_SITE_VERIFICATION;

export interface Props {
  title?: string;
  author?: string;
  profile?: string;
  description?: string;
  ogImage?: string;
  canonicalURL?: string;
  pubDatetime?: Date;
  modDatetime?: Date | null;
  scrollSmooth?: boolean;
}

const {
  title = SITE.title,
  author = SITE.author,
  profile = SITE.profile,
  description = SITE.desc,
  ogImage = SITE.ogImage ? `/${SITE.ogImage}` : "/og.png",
  canonicalURL = new URL(Astro.url.pathname, Astro.url),
  pubDatetime,
  modDatetime,
  scrollSmooth = false,
} = Astro.props;

const socialImageURL = new URL(ogImage, Astro.url);

const structuredData = {
  "@context": "https://schema.org",
  "@type": "BlogPosting",
  headline: `${title}`,
  image: `${socialImageURL}`,
  datePublished: `${pubDatetime?.toISOString()}`,
  ...(modDatetime && { dateModified: modDatetime.toISOString() }),
  author: [
    {
      "@type": "Person",
      name: `${author}`,
      ...(profile && { url: profile }),
    },
  ],
};
---

<!doctype html>
<html lang=`${SITE.lang ?? "en"}` class={`${scrollSmooth && "scroll-smooth"}`}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="view-transition" content="same-origin" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="canonical" href={canonicalURL} />
    <meta name="generator" content={Astro.generator} />

    <!-- General Meta Tags -->
    <title>{title}</title>
    <meta name="title" content={title} />
    <meta name="description" content={description} />
    <meta name="author" content={author} />
    <link rel="sitemap" href="/sitemap-index.xml" />

    <!-- Open Graph / Facebook -->
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:image" content={socialImageURL} />

    <!-- Article Published/Modified time -->
    {
      pubDatetime && (
        <meta
          property="article:published_time"
          content={pubDatetime.toISOString()}
        />
      )
    }
    {
      modDatetime && (
        <meta
          property="article:modified_time"
          content={modDatetime.toISOString()}
        />
      )
    }

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalURL} />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={socialImageURL} />

    <!-- Google JSON-LD Structured data -->
    <script
      type="application/ld+json"
      is:inline
      set:html={JSON.stringify(structuredData)}
    />

    <!-- Enable RSS feed auto-discovery  -->
    <!-- https://docs.astro.build/en/recipes/rss/#enabling-rss-feed-auto-discovery -->
    <link
      rel="alternate"
      type="application/rss+xml"
      title={SITE.title}
      href={`${SITE.website}rss.xml`}
    />

    <meta name="theme-color" content="" />

    {
      // If PUBLIC_GOOGLE_SITE_VERIFICATION is set in the environment variable,
      // include google-site-verification tag in the heading
      // Learn more: https://support.google.com/webmasters/answer/9008080#meta_tag_verification&zippy=%2Chtml-tag
      googleSiteVerification && (
        <meta
          name="google-site-verification"
          content={googleSiteVerification}
        />
      )
    }

    <ViewTransitions />

    <script is:inline src="/toggle-theme.js"></script>
    
    <!-- Domain-based markdown redirect -->
    <script is:inline>
      // Check if we're on carlosluengo.md domain
      const hostname = window.location.hostname;
      if ((hostname === 'carlosluengo.md' || hostname === 'www.carlosluengo.md') && !window.location.pathname.endsWith('.md')) {
        // Redirect to the .md version of the current page
        const mdPath = window.location.pathname === '/' ? '/index.md' : window.location.pathname + '.md';
        window.location.replace(mdPath + window.location.search + window.location.hash);
      }
    </script>
    
    <!-- Twitter Widget Script -->
    <script async src="https://platform.twitter.com/widgets.js" charset="utf-8"></script>
    
    <!-- Cloudflare AI Search (NLWeb) -->
    <link rel="stylesheet" href="https://frosty-boat-36a8-nlweb.carluve.workers.dev/nlweb-dropdown-chat.css">
    <link rel="stylesheet" href="https://frosty-boat-36a8-nlweb.carluve.workers.dev/common-chat-styles.css">
  </head>
  <body>
    <slot />
    <Analytics />
    
    <!-- AI Chat Bot Button -->
    <div id="ai-chat-wrapper">
      <!-- Welcome bubble -->
      <div id="ai-chat-bubble" class="ai-chat-bubble">
        <span>👋 ¡Hola! Pregúntame lo que quieras sobre el blog</span>
        <button id="ai-bubble-close" class="ai-bubble-close">×</button>
      </div>
      
      <!-- Chat container (hidden by default) -->
      <div id="ai-chat-panel" class="ai-chat-panel">
        <div class="ai-chat-header">
          <span>🤖 AI Assistant</span>
          <button id="ai-panel-close" class="ai-panel-close">×</button>
        </div>
        <div id="docs-search-container"></div>
      </div>
      
      <!-- Floating bot button -->
      <button id="ai-chat-btn" class="ai-chat-btn" aria-label="Abrir asistente AI">
        <svg id="ai-btn-icon" xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M12 8V4H8"/>
          <rect width="16" height="12" x="4" y="8" rx="2"/>
          <path d="M2 14h2"/>
          <path d="M20 14h2"/>
          <path d="M15 13v2"/>
          <path d="M9 13v2"/>
        </svg>
        <svg id="ai-btn-close" class="hidden" xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M18 6 6 18"/>
          <path d="m6 6 12 12"/>
        </svg>
      </button>
    </div>
    
    <!-- Cloudflare AI Search Script -->
    <script type="module">
      import { NLWebDropdownChat } from 'https://frosty-boat-36a8-nlweb.carluve.workers.dev/nlweb-dropdown-chat.js';

      const chat = new NLWebDropdownChat({
        containerId: 'docs-search-container',
        site: 'https://frosty-boat-36a8-nlweb.carluve.workers.dev',
        placeholder: 'Escribe tu pregunta...',
        endpoint: 'https://frosty-boat-36a8-nlweb.carluve.workers.dev'
      });
      
      // UI Elements
      const chatBtn = document.getElementById('ai-chat-btn');
      const chatPanel = document.getElementById('ai-chat-panel');
      const chatBubble = document.getElementById('ai-chat-bubble');
      const bubbleClose = document.getElementById('ai-bubble-close');
      const panelClose = document.getElementById('ai-panel-close');
      const btnIcon = document.getElementById('ai-btn-icon');
      const btnClose = document.getElementById('ai-btn-close');
      
      let isOpen = false;
      
      // Show bubble after 3 seconds (only once per session)
      if (!sessionStorage.getItem('ai-bubble-shown')) {
        setTimeout(() => {
          chatBubble?.classList.add('show');
          sessionStorage.setItem('ai-bubble-shown', 'true');
        }, 3000);
      }
      
      // Close bubble
      bubbleClose?.addEventListener('click', (e) => {
        e.stopPropagation();
        chatBubble?.classList.remove('show');
      });
      
      // Toggle chat panel
      chatBtn?.addEventListener('click', () => {
        isOpen = !isOpen;
        chatPanel?.classList.toggle('show', isOpen);
        chatBubble?.classList.remove('show');
        btnIcon?.classList.toggle('hidden', isOpen);
        btnClose?.classList.toggle('hidden', !isOpen);
      });
      
      // Close panel
      panelClose?.addEventListener('click', () => {
        isOpen = false;
        chatPanel?.classList.remove('show');
        btnIcon?.classList.remove('hidden');
        btnClose?.classList.add('hidden');
      });
      
      // Close on click outside
      document.addEventListener('click', (e) => {
        const wrapper = document.getElementById('ai-chat-wrapper');
        if (isOpen && wrapper && !wrapper.contains(e.target)) {
          isOpen = false;
          chatPanel?.classList.remove('show');
          btnIcon?.classList.remove('hidden');
          btnClose?.classList.add('hidden');
        }
      });
    </script>
  </body>
</html>

<style>
  html,
  body {
    margin: 0;
    width: 100%;
    height: 100%;
  }
</style>
